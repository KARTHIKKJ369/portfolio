## 2026-07-10 - Added tooltips and ARIA labels to icon-only nav links\n**Learning:** Icon-only navigation links in the `Navbar` component were missing accessible names for screen readers and tooltips for mouse users, a common pattern in minimalistic designs.\n**Action:** Always verify icon-only anchor tags and buttons have descriptive `aria-label` and `title` attributes to ensure full accessibility.
